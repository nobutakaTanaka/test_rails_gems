require "spec_helper"

describe CryptAuth do
  it "has a version number" do
    expect(CryptAuth::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
